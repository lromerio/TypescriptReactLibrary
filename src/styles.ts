import {types} from "typestyle";

export namespace Base {
    export const text: types.NestedCSSProperties = {
        color: "#222",
        fontFamily: "Roboto Regular",
        fontSize: "13px",
    };
}
